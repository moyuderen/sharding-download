{
  "name": "sharding-download",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "docs:build": "pnpm run -C docs docs:build",
    "change": "pnpm changeset",
    "change-version": "pnpm changeset version",
    "publish": "pnpm install && pnpm build -F sharding-download && pnpm publish -F sharding-download"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.29.3"
  }
}